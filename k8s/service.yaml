apiVersion: v1
kind: Service
metadata:
  name: plant-disease-service
spec:
  type: NodePort
  selector:
    app: plant-disease
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30080
